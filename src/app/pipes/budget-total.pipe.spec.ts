import { BudgetTotalPipe } from './budget-total.pipe';

describe('BudgetTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new BudgetTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
